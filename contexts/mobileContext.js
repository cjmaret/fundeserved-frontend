/* this is to lift the state so that for example the featured section can remove the bottom three cards when its mobile */

import { createContext } from 'react';

export const MobileContext = createContext();
